// NPOC deployment bootstrap config.
// Fill these once before pushing to GitHub if you want all browsers/devices to auto-connect.
window.NPOC_CONFIG = {
  mode: 'local',
  backendUrl: '',
  apiKey: '',
  sheetId: '',
  allowedDomain: 'rfs.org'
};
